# .htaccess für /install/ Verzeichnis
# ALFAHOSTING-KOMPATIBEL (PHP-FPM)

# Zeichenkodierung
AddDefaultCharset UTF-8

# Verzeichnis-Listing deaktivieren
Options -Indexes

# DirectoryIndex
DirectoryIndex install.php

# Sicherheit: Nur install.php ist zugänglich (Apache 2.4 Syntax)
<FilesMatch "^(database_complete\.sql|\.user\.ini|README\.md|ALFAHOSTING_ANLEITUNG\.md)$">
    Require all denied
</FilesMatch>

# Erlaube Zugriff auf install.php (Apache 2.4 Syntax)
<Files "install.php">
    Require all granted
</Files>
